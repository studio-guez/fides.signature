services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    user: "1000:1000"
    environment:
      - NODE_ENV=development
      - NITRO_HOST=0.0.0.0
